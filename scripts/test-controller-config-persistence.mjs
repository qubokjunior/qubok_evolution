import { readFileSync } from "node:fs";
import { dirname, resolve } from "node:path";
import { fileURLToPath } from "node:url";
const projectRoot = resolve(dirname(fileURLToPath(import.meta.url)), "..");
const readText = (relativePath) => readFileSync(resolve(projectRoot, relativePath), "utf8");
const assert = (condition, message) => { if (!condition) throw new Error(message); };
const packageJson = JSON.parse(readText("package.json"));
const controllerPersistence = readText("src/sim/controllerConfigPersistence.ts");
const bridgePersistence = readText("src/sim/controllerActuatorConfigPersistence.ts");
assert(packageJson.scripts["test:controller-config-persistence"] === "node scripts/test-controller-config-persistence.mjs", "package.json must expose test:controller-config-persistence.");
assert(packageJson.scripts.test.includes("test:controller-config-persistence"), "npm run test must include test:controller-config-persistence.");
for (const token of ["CONTROLLER_CONFIG_STORAGE_KEY", "qubok_evolve.controller_config.v1", "loadStoredControllerConfig", "saveControllerConfig", "clearStoredControllerConfig", "enableController", "controllerStrength", "controllerMaxIntentPerAgent", "controllerFoodWeight", "controllerThreatWeight", "controllerFlowWeight"]) assert(controllerPersistence.includes(token), "controllerConfigPersistence missing token: " + token);
for (const token of ["CONTROLLER_ACTUATOR_CONFIG_STORAGE_KEY", "qubok_evolve.controller_actuator_config.v1", "loadStoredControllerActuatorConfig", "saveControllerActuatorConfig", "clearStoredControllerActuatorConfig", "enableControllerMovementInfluence", "controllerForceScale", "controllerMaxForce", "controllerMinActiveIntentMagnitude"]) assert(bridgePersistence.includes(token), "controller actuator persistence missing token: " + token);
console.log("controller config persistence tests passed");
