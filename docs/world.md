# world
## m22 obstacle-aware agent spawning

Agents can now be initialized through `spawnRandomAgentsAvoidingObstacles()`. The world state layout and `spawnAgent()` remain unchanged; obstacle validation is handled externally by `spawnValidation.ts`.
